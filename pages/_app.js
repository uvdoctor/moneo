import '../styles/index.css'

export default function App({ Component, pageProps }) {
    return (
        <div>
            <p className="font-black text-4xl">Your Financial Analyst</p>
        </div>
    );

    //return <Component {...pageProps} />
}