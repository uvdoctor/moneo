enum LMH {
  L, M, H
}

enum YN {
  Y, N
}

type Target {
  month: Int
  year: Int!
  val: Int!
  curr: String!
  met: YN
  prob: LMH
}

type Goal @model @auth(rules: [{allow: owner}]) {
  id: ID!
  name: String!
  targets: [Target!]!
  milestones: [Target]
  imp: LMH!
  met: YN
  prob: LMH
}